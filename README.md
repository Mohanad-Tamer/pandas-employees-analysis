# pandas-employees-analysis
This project simulates a small-scale data engineering pipeline for employee data using Python. The workflow includes generating synthetic employee data, storing it in JSON format, loading it into Pandas for cleaning and transformation, building a simple search functionality, and generating analytical reports exported back to JSON.
